# Система многоязычности сайта

## 🌍 Реализованные языки

Система поддерживает **3 языка**:

1. **🇷🇺 Русский** (по умолчанию)
2. **🇺🇸 English** 
3. **🇧🇾 Беларуская**

## 🎯 Функционал

### ✅ Что переводится:

**Навигация и меню:**
- Все пункты главного меню
- Подменю (о предприятии, новости, деятельность и т.д.)
- Кнопки (войти, выйти, поиск)

**Cookie баннер:**
- Заголовок и описание
- Кнопки (принять все, отклонить, настроить)
- Типы cookie (необходимые, функциональные, аналитические, маркетинговые)

**Контактная информация:**
- Адрес и время работы
- Разделы контактов

**Админ панель:**
- Все названия разделов управления
- Кнопки и интерфейс

### 🔧 Как работает:

1. **Переключатель языков** - кнопка с глобусом и кодом языка (RU/EN/BE)
2. **Сохранение выбора** - язык сохраняется в localStorage
3. **Автоматическое применение** - все тексты меняются мгновенно
4. **HTML lang атрибут** - обновляется для SEO

## 🎨 Интерфейс переключателя

### Расположение:
- **Desktop:** В левом сайдбаре под полем поиска
- **Mobile:** В мобильном меню

### Внешний вид:
```
┌─────────────┐
│     🌍      │  ← Иконка глобуса
│     RU      │  ← Код текущего языка
└─────────────┘
```

### Диалог выбора:
```
┌─────────────────────────────────────┐
│  🌍 Выберите язык                   │
├─────────────────────────────────────┤
│  🇷🇺  Русский              ✓      │
│  🇺🇸  English                      │
│  🇧🇾  Беларуская                  │
├─────────────────────────────────────┤
│  Текущий язык: Русский              │
│  Язык сохраняется в браузере        │
└─────────────────────────────────────┘
```

## 🔄 Как использовать

### Для пользователей:

1. **Смена языка:**
   - Нажмите на кнопку с глобусом в сайдбаре
   - Выберите нужный язык из списка
   - Язык изменится мгновенно

2. **Сохранение:**
   - Выбор сохраняется автоматически
   - При следующем посещении язык будет тот же

3. **Сброс языка:**
   ```javascript
   // В консоли браузера
   localStorage.removeItem('language');
   window.location.reload();
   ```

### Для разработчиков:

**Добавление нового перевода:**
```typescript
// В LanguageContext.tsx
const translations = {
  ru: {
    'new_key': 'Русский текст',
    // ...
  },
  en: {
    'new_key': 'English text',
    // ...
  },
  be: {
    'new_key': 'Беларускі тэкст',
    // ...
  }
};
```

**Использование в компоненте:**
```tsx
import { useLanguage } from '../contexts/LanguageContext';

function MyComponent() {
  const { t } = useLanguage();
  
  return <h1>{t('new_key')}</h1>;
}
```

## 📝 Структура переводов

### Категории ключей:

**Общие (General):**
- `welcome`, `home`, `search`, `login`, `logout`

**Навигация (Navigation):**
- `about`, `news`, `activity`, `air_navigation`, `services`

**О предприятии (About):**
- `about_company`, `management`, `structure`, `branches`, `vacancies`

**Новости (News):**
- `all_news`, `company_news`, `flight_safety`, `information_security`

**Деятельность (Activity):**
- `international`, `standards`, `hr_policy`, `information_systems`

**Аэронавигация (Air Navigation):**
- `eaip`, `eaip_vfr`, `etod`, `notam`, `air_traffic_services`

**Контакты (Contacts):**
- `address`, `phone`, `email`, `working_hours`, `minsk_address`

**Cookie:**
- `cookie_title`, `cookie_message`, `accept_all`, `reject`, `settings`

**Админ панель (Admin):**
- `admin_panel`, `user_management`, `role_management`, `news_management`

## 🎯 Техническая реализация

### Файлы системы:

1. **`LanguageContext.tsx`** - контекст и переводы
2. **`LanguageSwitcher.tsx`** - компонент переключателя
3. **`main.tsx`** - провайдер контекста
4. **`Sidebar.tsx`** - интеграция переключателя

### Хранение языка:
```javascript
// localStorage
localStorage.setItem('language', 'ru'); // или 'en', 'be'
```

### HTML lang атрибут:
```html
<html lang="ru"> <!-- автоматически обновляется -->
```

## 🌐 SEO оптимизация

### Автоматические изменения:
- **HTML lang атрибут** - обновляется при смене языка
- **Мета теги** - можно добавить hreflang для поисковых систем

### Рекомендации:
1. Добавить hreflang в `<head>`:
```html
<link rel="alternate" hreflang="ru" href="/" />
<link rel="alternate" hreflang="en" href="/" />
<link rel="alternate" hreflang="be" href="/" />
```

2. Добавить meta description на разных языках:
```html
<meta name="description" content="..." lang="ru" />
<meta name="description" content="..." lang="en" />
<meta name="description" content="..." lang="be" />
```

## 📱 Адаптивность

### Desktop (≥951px):
- Переключатель в левом сайдбаре
- Полный диалог выбора языка

### Mobile (<951px):
- Компактная версия в мобильном меню
- Адаптированный диалог

## 🔧 Добавление новых языков

### Шаг 1: Добавить язык в типы
```typescript
export type Language = 'ru' | 'en' | 'be' | 'de'; // добавить 'de'
```

### Шаг 2: Добавить переводы
```typescript
const translations = {
  // ... существующие языки
  de: {
    'welcome': 'Willkommen',
    // ... все ключи
  }
};
```

### Шаг 3: Добавить в список языков
```typescript
const languageOptions = [
  // ... существующие
  { code: 'de' as Language, name: 'Deutsch', flag: '🇩🇪' },
];
```

## 🎨 Кастомизация

### Изменение внешнего вида:
```tsx
// В LanguageSwitcher.tsx
<Button
  variant="ghost"
  size="sm"
  className="custom-language-button" // добавить свой класс
>
```

### Добавление анимации:
```css
.language-switcher {
  transition: all 0.3s ease;
}

.language-switcher:hover {
  transform: scale(1.05);
}
```

## 🚀 Готово!

Система многоязычности полностью реализована и готова к использованию! 🎉

### Что работает:
- ✅ Переключение между 3 языками
- ✅ Сохранение выбора пользователя
- ✅ Перевод всех основных элементов интерфейса
- ✅ Адаптивный дизайн
- ✅ SEO оптимизация
- ✅ Простое добавление новых языков

### Как проверить:
1. Откройте сайт
2. Нажмите на кнопку с глобусом в сайдбаре
3. Выберите другой язык
4. Все тексты должны измениться мгновенно
5. Обновите страницу - язык должен сохраниться

Система готова к использованию! 🌍
