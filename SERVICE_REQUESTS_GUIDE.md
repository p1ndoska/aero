# Руководство по системе заявок на услуги

## ✅ Функционал реализован полностью!

Система заявок на услуги позволяет пользователям подавать заявки на конкретные услуги, а администраторам - управлять ими.

## 🏗️ Архитектура системы

### Backend
- **Модель**: `ServiceRequest` в Prisma схеме
- **Контроллер**: `ServiceRequestController.js`
- **API маршруты**: `/api/service-requests/*`
- **Email уведомления**: Автоматическая отправка при создании заявки

### Frontend
- **Форма заявки**: `ServiceRequestForm.tsx`
- **Админ-панель**: `ServiceRequestManagement.tsx`
- **Интеграция**: В `ServicesPage.tsx` и `DynamicPage.tsx`

## 📋 Функциональность

### Для пользователей:
1. **Подача заявки** - кнопка "Подать заявку" на каждой услуге
2. **Заполнение формы** с полями:
   - ФИО, email, телефон (обязательные)
   - Организация, должность (опциональные)
   - Тема и описание заявки (обязательные)
   - Приоритет, предпочтительная дата, бюджет (опциональные)
   - Дополнительные заметки
3. **Email подтверждение** - автоматическое письмо после подачи

### Для администраторов:
1. **Просмотр всех заявок** с фильтрацией и поиском
2. **Статистика** - общее количество, по статусам, по типам услуг
3. **Управление заявками**:
   - Изменение статуса (ожидает, в работе, завершена, отменена)
   - Изменение приоритета (низкий, средний, высокий, срочный)
   - Добавление ответа клиенту
   - Внутренние заметки
4. **Email уведомления** - автоматическое письмо администратору о новой заявке

## 🎯 Где найти функционал

### В админ-панели:
- Вкладка **"Заявки на услуги"** (доступна для `SUPER_ADMIN` и `SERVICES_ADMIN`)
- Полная статистика и управление заявками

### На сайте:
- **Главная страница услуг** (`/services`) - кнопка "Подать заявку" на каждой карточке
- **Страницы конкретных услуг** (`/services/[pageType]`) - кнопка "Подать заявку" в заголовке

## 📧 Email уведомления

### Заявителю отправляется:
- Подтверждение получения заявки
- Номер заявки
- Детали заявки
- Информация о дальнейших действиях
- Контактная информация компании

### Администратору отправляется:
- Уведомление о новой заявке
- Полная информация о заявителе
- Детали заявки
- Приоритет и статус
- Призыв к действию

## 🔧 Технические детали

### API Endpoints:
- `POST /api/service-requests` - создание заявки (публичный)
- `GET /api/service-requests` - получение всех заявок (админ)
- `GET /api/service-requests/:id` - получение заявки по ID (админ)
- `PUT /api/service-requests/:id` - обновление заявки (админ)
- `DELETE /api/service-requests/:id` - удаление заявки (админ)
- `GET /api/service-requests-stats` - статистика заявок (админ)

### Роли доступа:
- **Публичный доступ**: Подача заявок
- **SERVICES_ADMIN**: Управление заявками на услуги
- **SUPER_ADMIN**: Полный доступ ко всем функциям

### Валидация:
- Обязательные поля: ФИО, email, тема, описание
- Валидация email формата
- Автоматическая установка статуса "ожидает" и приоритета "средний"

## 🎨 UI/UX особенности

- **Многоязычность**: Поддержка RU, EN, BE
- **Адаптивный дизайн**: Работает на всех устройствах
- **Валидация форм**: Браузерная валидация + toast уведомления
- **Интуитивный интерфейс**: Понятные кнопки и формы
- **Цветовая схема**: Соответствует общему дизайну сайта (#213659)

## 🚀 Готово к использованию!

Система полностью функциональна и готова к использованию. Пользователи могут подавать заявки, администраторы - управлять ими, все уведомления работают автоматически.

## 📞 Поддержка

При возникновении вопросов или проблем обращайтесь к разработчику системы.

