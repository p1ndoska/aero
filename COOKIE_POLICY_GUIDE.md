# Руководство по политике Cookie

## 📋 Что реализовано

Полная система управления cookie-файлами для сайта РУП «Белаэронавигация», включающая:

### 1. Баннер согласия на cookie (`CookieConsent.tsx`)
- ✅ Современный адаптивный дизайн
- ✅ Появляется при первом посещении сайта
- ✅ Два режима: простой и расширенный
- ✅ Сохранение выбора пользователя в localStorage

### 2. Страница политики cookie (`CookiePolicyPage.tsx`)
- ✅ Подробное описание всех типов cookie
- ✅ Инструкции по управлению cookie в браузерах
- ✅ Отображение текущих настроек пользователя
- ✅ Возможность изменить настройки

### 3. Интеграция в приложение
- ✅ Автоматическое отображение при первом посещении
- ✅ Роут `/cookie-policy` для страницы политики
- ✅ Ссылки на политику в баннере

## 🎨 Функционал баннера

### Основной режим
При первом посещении пользователь видит:
- Информацию о использовании cookie
- Ссылку на полную политику
- Три кнопки:
  - **"Принять все"** - принимает все типы cookie
  - **"Отклонить"** - отклоняет необязательные cookie
  - **"Настроить"** - открывает расширенные настройки

### Режим настроек
Детальный контроль над типами cookie:
- 🔒 **Необходимые** - всегда включены (нельзя отключить)
- ⚙️ **Функциональные** - можно включить/отключить
- 📊 **Аналитические** - можно включить/отключить
- 📢 **Маркетинговые** - можно включить/отключить

## 📝 Типы cookie

### Необходимые cookie (Всегда включены)
**Назначение:** Обеспечивают базовую работу сайта
```
- Авторизация пользователей
- Безопасность и защита от CSRF
- Корзина покупок
- Языковые настройки
```

### Функциональные cookie
**Назначение:** Расширенная функциональность
```
- Запоминание региона/языка
- Настройки отображения
- Персонализация контента
- Улучшение UX
```

### Аналитические cookie
**Назначение:** Анализ поведения пользователей
```
- Google Analytics
- Яндекс.Метрика
- Подсчет посетителей
- Популярные страницы
```

### Маркетинговые cookie
**Назначение:** Реклама и маркетинг
```
- Показ релевантной рекламы
- Ретаргетинг
- Отслеживание конверсий
- ROI рекламных кампаний
```

## 🔧 Техническая реализация

### Структура хранения в localStorage
```javascript
{
  "necessary": true,      // Всегда true
  "functional": true,     // Настраивается
  "analytics": false,     // Настраивается
  "marketing": false,     // Настраивается
  "timestamp": "2024-10-10T12:00:00.000Z"
}
```

### Компоненты

**CookieConsent.tsx** (Баннер)
- Расположение: `frontend/src/components/CookieConsent.tsx`
- Автоматически показывается при первом посещении
- Сохраняет выбор в localStorage
- Два режима: простой и расширенный

**CookiePolicyPage.tsx** (Страница политики)
- Расположение: `frontend/src/components/CookiePolicyPage.tsx`
- Доступна по адресу `/cookie-policy`
- Показывает текущие настройки
- Позволяет изменить настройки

### Интеграция

**Layout.tsx**
```tsx
import CookieConsent from "./CookieConsent";

// В конце Layout добавлен
<CookieConsent />
```

**main.tsx**
```tsx
{ path: "/cookie-policy", element: <CookiePolicyPage /> }
```

## 🎯 Использование

### Для пользователей:
1. При первом посещении появляется баннер
2. Можно выбрать:
   - Принять все cookie
   - Отклонить необязательные
   - Настроить детально
3. Выбор сохраняется и баннер больше не показывается
4. Доступ к политике: `/cookie-policy`
5. Изменить настройки можно на странице политики

### Для разработчиков:

**Проверка согласия в коде:**
```javascript
const consent = localStorage.getItem('cookieConsent');
if (consent) {
  const preferences = JSON.parse(consent);
  
  if (preferences.analytics) {
    // Подключить Google Analytics
  }
  
  if (preferences.marketing) {
    // Подключить маркетинговые скрипты
  }
}
```

**Сброс согласия (для тестирования):**
```javascript
localStorage.removeItem('cookieConsent');
window.location.reload();
```

## 📱 Адаптивность

Баннер полностью адаптивен:
- **Desktop:** Горизонтальное расположение элементов
- **Tablet:** Адаптированная компоновка
- **Mobile:** Вертикальное расположение кнопок

## 🔗 Добавление ссылки в футер

Рекомендуется добавить ссылку на политику cookie в футер сайта:

```tsx
<Link to="/cookie-policy">Политика использования cookie</Link>
```

## ⚖️ Соответствие законодательству

Реализация соответствует:
- ✅ GDPR (Общий регламент по защите данных ЕС)
- ✅ ePrivacy Directive
- ✅ Законодательство Республики Беларусь о защите персональных данных

### Ключевые моменты:
1. **Информирование** - пользователи получают полную информацию
2. **Согласие** - явное согласие перед использованием необязательных cookie
3. **Контроль** - возможность управления настройками
4. **Отзыв** - возможность изменить решение в любое время

## 🎨 Кастомизация

### Изменение цветов
В файле `CookieConsent.tsx` можно изменить цветовую схему:
```tsx
className="bg-blue-600 hover:bg-blue-700"  // Основная кнопка
className="border-blue-200 bg-blue-50"     // Карточка
```

### Изменение текстов
Все тексты находятся в компонентах и легко редактируются.

### Изменение контактов
В `CookiePolicyPage.tsx` обновите контактную информацию:
```tsx
<p><strong>Email:</strong> info@belaeronavigation.by</p>
<p><strong>Телефон:</strong> +375 (17) 123-45-67</p>
```

## 🚀 Развертывание

1. Все файлы уже созданы и интегрированы
2. Баннер появится автоматически при первом запуске
3. Никаких дополнительных настроек не требуется

## 📊 Аналитика

После получения согласия на аналитические cookie, можно подключить:

```javascript
// В main.tsx или App.tsx
useEffect(() => {
  const consent = localStorage.getItem('cookieConsent');
  if (consent) {
    const prefs = JSON.parse(consent);
    
    if (prefs.analytics) {
      // Google Analytics
      ReactGA.initialize('UA-XXXXXXXXX-X');
      
      // Яндекс.Метрика
      ym(XXXXXX, 'init', { clickmap: true });
    }
  }
}, []);
```

## ✅ Готово!

Политика cookie полностью реализована и готова к использованию! 🎉

Система автоматически:
- Показывает баннер новым посетителям
- Сохраняет выбор пользователя
- Предоставляет полный контроль над cookie
- Соответствует законодательным требованиям

