# Исправление проблем с добавлением филиалов

## 🐛 **Проблемы, которые были исправлены**

### ✅ **1. Неправильный порт в константах**
**Проблема:** Frontend пытался подключиться к порту 8000, но backend работает на порту 3000.

**Исправление:**
```typescript
// frontend/src/constants/index.ts
export const BASE_URL = 'http://localhost:3000';  // было 8000
export const SERVER_URL = 'http://localhost:3000'; // было 8000
```

### ✅ **2. Проблемы с обработкой JSON content**
**Проблема:** Поле `content` в базе данных хранится как JSON, но frontend отправлял массив объектов.

**Исправления:**

#### **В handleCreate и handleUpdate:**
```typescript
// Преобразуем content в JSON строку для отправки
const dataToSend = {
  ...formData,
  content: formData.content ? JSON.stringify(formData.content) : null
};
```

#### **В handleEdit:**
```typescript
// Парсим content из JSON строки
let parsedContent = [];
try {
  if (branch.content) {
    if (typeof branch.content === 'string') {
      parsedContent = JSON.parse(branch.content);
    } else {
      parsedContent = branch.content;
    }
  }
} catch (error) {
  console.error('Ошибка парсинга content:', error);
  parsedContent = [];
}
```

#### **В renderContentPreview:**
```typescript
const renderContentPreview = (content: any) => {
  let parsedContent = [];
  try {
    if (typeof content === 'string') {
      parsedContent = JSON.parse(content);
    } else {
      parsedContent = content;
    }
  } catch (error) {
    return <span>Ошибка загрузки контента</span>;
  }
  // ... остальная логика
};
```

### ✅ **3. Улучшенная обработка ошибок**
**Добавлено:**
- `console.error` для отладки
- Более детальные сообщения об ошибках
- Защита от ошибок парсинга JSON

## 🔧 **Технические детали**

### **Проблема с типами данных:**
- **Backend:** `content` хранится как `Json` в Prisma
- **Frontend:** `content` обрабатывается как массив `ContentElement[]`
- **Решение:** Сериализация/десериализация при передаче данных

### **Структура данных content:**
```typescript
interface ContentElement {
  id: string;
  type: 'paragraph' | 'heading' | 'link' | 'image';
  content: string;
  props?: {
    level?: number;
    href?: string;
    target?: string;
    alt?: string;
    src?: string;
  };
}
```

## 🚀 **Как проверить исправления**

### **1. Запустите backend:**
```bash
cd backend && npm start
```

### **2. Убедитесь, что backend работает на порту 3000:**
- Проверьте `backend/bin/www` - там должен быть порт 3000
- Проверьте консоль - должно быть "Listening on port 3000"

### **3. Проверьте frontend:**
- Убедитесь, что `BASE_URL` в константах указывает на порт 3000
- Откройте админ панель
- Перейдите на вкладку "Управление филиалами"
- Попробуйте создать филиал

### **4. Проверьте в браузере:**
- Откройте DevTools (F12)
- Перейдите на вкладку Network
- Попробуйте создать филиал
- Проверьте, что запросы идут на `localhost:3000/api/branch`

## 📋 **Возможные проблемы и решения**

### **Если все еще не работает:**

1. **Проверьте токен аутентификации:**
   - Убедитесь, что пользователь авторизован
   - Проверьте роль пользователя (должна быть SUPER_ADMIN)

2. **Проверьте CORS:**
   - Убедитесь, что CORS настроен в backend
   - Проверьте, что frontend URL разрешен

3. **Проверьте логи backend:**
   - Смотрите консоль backend сервера
   - Ищите ошибки при создании филиала

4. **Проверьте базу данных:**
   - Убедитесь, что миграция применена
   - Проверьте, что таблица Branch существует

## ✅ **Результат**

После исправлений:
- ✅ Правильный порт для API запросов
- ✅ Корректная обработка JSON данных
- ✅ Улучшенная обработка ошибок
- ✅ Защита от ошибок парсинга
- ✅ Добавление филиалов должно работать

Теперь добавление филиалов должно работать корректно! 🎉



