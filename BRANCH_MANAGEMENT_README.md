# Система управления филиалами с конструктором контента

## 🎯 Что было реализовано

### ✅ **Backend API (Node.js + Prisma + PostgreSQL)**

1. **Обновлена схема базы данных:**
   - Добавлено поле `content` типа `Json` в модель `Branch`
   - Создана миграция `20251005190136_add_content_to_branch`

2. **Создан контроллер филиалов (`BranchController.js`):**
   - `getAllBranches()` - получить все филиалы
   - `getBranchById(id)` - получить филиал по ID
   - `createBranch(data)` - создать новый филиал
   - `updateBranch(id, data)` - обновить филиал
   - `deleteBranch(id)` - удалить филиал

3. **Добавлены API маршруты:**
   - `POST /branch/create` - создание филиала
   - `GET /branch` - получение всех филиалов
   - `GET /branch/:id` - получение филиала по ID
   - `PUT /branch/:id` - обновление филиала
   - `DELETE /branch/:id` - удаление филиала
   - Все маршруты защищены аутентификацией и требуют роль `SUPER_ADMIN`

### ✅ **Frontend (React + TypeScript + Redux Toolkit Query)**

1. **TypeScript типы (`types/branch.ts`):**
   ```typescript
   interface ContentElement {
     id: string;
     type: 'paragraph' | 'heading' | 'link' | 'image';
     content: string;
     props?: {
       level?: number;    // Для заголовков (1-6)
       href?: string;     // Для ссылок
       target?: string;   // Для ссылок
       alt?: string;      // Для изображений
       src?: string;      // Для изображений
     };
   }
   ```

2. **API сервис (`services/branchApi.ts`):**
   - Интеграция с Redux Toolkit Query
   - Хуки для всех CRUD операций
   - Автоматическое кеширование и синхронизация

3. **Конструктор контента (`ContentConstructor.tsx`):**
   - **Добавление элементов:** заголовки, абзацы, ссылки, изображения
   - **Редактирование:** inline редактирование каждого элемента
   - **Управление порядком:** кнопки "Вверх"/"Вниз"
   - **Предварительный просмотр:** live preview контента
   - **Валидация:** проверка обязательных полей

4. **Управление филиалами (`BranchManagement.tsx`):**
   - **Создание:** форма с валидацией + конструктор контента
   - **Редактирование:** модальные окна с предзаполненными данными
   - **Удаление:** подтверждение + безопасное удаление
   - **Просмотр:** карточки с превью контента и изображений

5. **Интеграция в админ панель:**
   - Добавлена вкладка "Управление филиалами" в `SuperAdminDashboard`
   - Иконка `Building2` из Lucide React
   - Полная интеграция с существующей системой табов

### 🎨 **Конструктор контента - возможности**

#### **Типы элементов:**

1. **Заголовки (H1-H6):**
   - Выбор уровня заголовка
   - Настраиваемый текст
   - Семантическая разметка

2. **Абзацы:**
   - Текстовый контент
   - Автоматическое форматирование

3. **Ссылки:**
   - Настраиваемый текст и URL
   - Выбор цели открытия (новая/текущая вкладка)
   - Валидация URL

4. **Изображения:**
   - Загрузка по URL
   - Альтернативный текст для доступности
   - Автоматическое масштабирование
   - Предварительный просмотр

#### **Управление контентом:**

- **Drag & Drop порядок:** изменение последовательности элементов
- **Inline редактирование:** редактирование без перезагрузки
- **Предварительный просмотр:** live preview в режиме реального времени
- **Валидация:** проверка корректности данных
- **Удаление:** безопасное удаление с подтверждением

### 🔧 **Технические особенности**

1. **Архитектура:**
   - Модульная структура компонентов
   - Переиспользуемые UI компоненты
   - Типизированные API интерфейсы

2. **UX/UI:**
   - Адаптивный дизайн
   - Интуитивный интерфейс
   - Быстрые операции без перезагрузки
   - Уведомления об успехе/ошибках

3. **Производительность:**
   - Кеширование данных через RTK Query
   - Оптимистичные обновления
   - Ленивая загрузка компонентов

4. **Безопасность:**
   - Аутентификация и авторизация
   - Валидация на клиенте и сервере
   - Защита от XSS через типизацию

### 📱 **Интерфейс пользователя**

#### **Список филиалов:**
- Карточки с основной информацией
- Превью контента (первые 2 элемента)
- Быстрые действия (редактировать/удалить)
- Превью изображений

#### **Создание/редактирование:**
- Модальные окна с формой
- Конструктор контента встроен в форму
- Валидация в реальном времени
- Сохранение прогресса

#### **Конструктор:**
- Кнопки добавления элементов
- Карточки с настройками каждого элемента
- Предварительный просмотр
- Управление порядком

### 🚀 **Готово к использованию**

Система полностью интегрирована и готова к использованию:

1. **Backend:** API endpoints работают
2. **Frontend:** UI компоненты созданы и интегрированы
3. **База данных:** миграция применена
4. **Авторизация:** доступ только для SUPER_ADMIN
5. **Валидация:** проверка данных на всех уровнях

### 📋 **API Endpoints**

```
POST   /api/branch/create     - Создать филиал
GET    /api/branch           - Получить все филиалы
GET    /api/branch/:id       - Получить филиал по ID
PUT    /api/branch/:id       - Обновить филиал
DELETE /api/branch/:id       - Удалить филиал
```

### 🎯 **Следующие шаги (опционально)**

- Загрузка изображений через файловую систему
- Расширенный редактор текста (WYSIWYG)
- Шаблоны контента
- Экспорт/импорт филиалов
- Массовые операции

Система управления филиалами с конструктором контента полностью реализована и готова к использованию! 🎉



