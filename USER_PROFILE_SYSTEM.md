# 👤 Система личного кабинета пользователей

## ✅ Реализованный функционал

### 🗄️ База данных

#### Обновленная модель User:
```prisma
model User {
  id          Int       @id @default(autoincrement())
  email       String    @unique
  password    String
  firstName   String?
  lastName    String?
  middleName  String?   // Отчество
  phone       String?   @unique
  avatar      String?
  birthDate   DateTime? // Дата рождения
  gender      String?   // Пол (male, female, other)
  address     String?   // Адрес
  position    String?   // Должность
  department  String?   // Отдел
  bio         String?   // Биография
  preferences Json?     // Настройки пользователя
  isEmailVerified Boolean @default(false)
  emailVerificationToken String?
  resetPasswordToken String?
  resetPasswordExpires DateTime?
  isActive    Boolean   @default(true)
  lastLoginAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  roleId      Int
  role        Role @relation(fields: [roleId], references: [id])
}
```

### 🔧 Backend API

#### Контроллер UserProfileController:
- **GET /api/profile** - Получить профиль пользователя
- **PUT /api/profile** - Обновить профиль
- **POST /api/profile/avatar** - Обновить аватар
- **PUT /api/profile/password** - Изменить пароль
- **GET /api/profile/stats** - Получить статистику
- **DELETE /api/profile** - Удалить аккаунт

#### Основные функции:
```javascript
// Получение профиля
getProfile: async (req, res) => {
  // Возвращает полную информацию о пользователе
  // Безопасно исключает пароль и токены
}

// Обновление профиля
updateProfile: async (req, res) => {
  // Проверка уникальности телефона
  // Валидация данных
  // Обновление полей
}

// Загрузка аватара
updateAvatar: async (req, res) => {
  // Multer для обработки файлов
  // Сохранение пути к файлу
  // TODO: Удаление старого аватара
}

// Смена пароля
changePassword: async (req, res) => {
  // Проверка текущего пароля
  // Хеширование нового пароля
  // Обновление в БД
}

// Статистика пользователя
getUserStats: async (req, res) => {
  // Подсчет заполненности профиля
  // Информация о регистрации и входе
}

// Удаление аккаунта
deleteAccount: async (req, res) => {
  // Проверка пароля
  // Полное удаление пользователя
}
```

### 🎨 Frontend компоненты

#### UserProfile.tsx - Основной компонент:
```tsx
// Структура компонента
- Заголовок с навигацией
- Боковая панель с аватаром и статистикой
- Вкладки: Профиль, Настройки, Безопасность
- Формы редактирования
- Модальные окна для критических действий
```

#### Основные секции:

##### 1. **Боковая панель**
- Аватар с возможностью загрузки
- Имя пользователя и роль
- Прогресс заполненности профиля
- Статистика (регистрация, последний вход)

##### 2. **Вкладка "Профиль"**
- Личная информация (имя, фамилия, отчество)
- Контактные данные (телефон, адрес)
- Профессиональная информация (должность, отдел)
- Биография
- Кнопки редактирования/сохранения

##### 3. **Вкладка "Настройки"**
- Email (только для чтения)
- Роль пользователя
- Статус аккаунта
- Информация о подтверждении email

##### 4. **Вкладка "Безопасность"**
- Изменение пароля (модальное окно)
- Удаление аккаунта (модальное окно с подтверждением)

### 🌐 Многоязычность

#### Добавлены переводы для всех трех языков:

**Русский:**
- `profile`: "Личный кабинет"
- `personal_info`: "Личная информация"
- `change_password`: "Изменить пароль"
- `delete_account`: "Удалить аккаунт"

**English:**
- `profile`: "User Profile"
- `personal_info`: "Personal Information"
- `change_password`: "Change Password"
- `delete_account`: "Delete Account"

**Беларуская:**
- `profile`: "Асабісты кабінет"
- `personal_info`: "Асабістая інфармацыя"
- `change_password`: "Змяніць пароль"
- `delete_account`: "Выдаліць аккаунт"

### 🔐 Безопасность

#### Аутентификация:
- Все роуты защищены middleware `authenticationToken`
- Проверка JWT токена для каждого запроса
- Получение ID пользователя из токена

#### Валидация:
- Проверка уникальности телефона
- Валидация длины пароля (минимум 6 символов)
- Проверка совпадения паролей при смене

#### Безопасность данных:
- Пароли не возвращаются в API ответах
- Хеширование паролей с bcrypt
- Проверка текущего пароля перед изменением

### 🎯 API интеграция

#### RTK Query API:
```typescript
export const userProfileApi = createApi({
  reducerPath: 'userProfileApi',
  baseQuery: fetchBaseQuery({
    baseUrl: '/api',
    prepareHeaders: (headers, { getState }) => {
      const token = (getState() as RootState).auth.token;
      if (token) {
        headers.set('authorization', `Bearer ${token}`);
      }
      return headers;
    },
  }),
  tagTypes: ['UserProfile'],
  endpoints: (builder) => ({
    getProfile: builder.query<UserProfile, void>(),
    updateProfile: builder.mutation<UserProfile, UpdateProfileRequest>(),
    updateAvatar: builder.mutation<{ id: number; avatar: string }, FormData>(),
    changePassword: builder.mutation<{ message: string }, ChangePasswordRequest>(),
    getUserStats: builder.query<UserStats, void>(),
    deleteAccount: builder.mutation<{ message: string }, DeleteAccountRequest>(),
  }),
});
```

### 🎨 UI/UX особенности

#### Дизайн:
- Адаптивная верстка (desktop/mobile)
- Современный Material Design
- Цветовая схема бренда (#213659)
- Анимации и переходы

#### Интерактивность:
- Режим редактирования с кнопками сохранения/отмены
- Модальные окна для критических действий
- Прогресс-бар заполненности профиля
- Drag & drop для загрузки аватара

#### Уведомления:
- Toast уведомления об успехе/ошибке
- Валидация форм в реальном времени
- Подтверждения для опасных действий

### 📱 Навигация

#### Добавлена в Sidebar:
```tsx
<Link to="/profile" className="text-left">
  <Button variant="outline" size="sm" className="w-full flex items-center gap-1">
    <FaUserCircle size={12} />
    Личный кабинет
  </Button>
</Link>
```

#### Роутинг:
```tsx
{ path: "/profile", element: <UserProfile /> }
```

### 🔧 Технические детали

#### Зависимости:
- **Backend**: Prisma, bcrypt, multer, JWT
- **Frontend**: RTK Query, React Hook Form, Lucide Icons
- **UI**: Tailwind CSS, shadcn/ui компоненты

#### Файловая структура:
```
backend/
├── controllers/UserProfileController.js
├── routes/index.js (обновлен)
└── prisma/schema.prisma (обновлен)

frontend/
├── components/UserProfile.tsx
├── app/services/userProfileApi.ts
├── contexts/LanguageContext.tsx (обновлен)
├── components/Sidebar.tsx (обновлен)
├── main.tsx (обновлен)
└── store/index.ts (обновлен)
```

### 🚀 Возможности расширения

#### Планируемые функции:
1. **Email верификация** - отправка подтверждающих писем
2. **Восстановление пароля** - через email
3. **Настройки уведомлений** - управление предпочтениями
4. **История активности** - логи действий пользователя
5. **Экспорт данных** - скачивание профиля в PDF/JSON
6. **Двухфакторная аутентификация** - дополнительная безопасность

#### Дополнительные поля:
- Социальные сети
- Навыки и компетенции
- Языки
- Сертификаты
- Образование
- Опыт работы

### ✅ Готово к использованию

Личный кабинет полностью интегрирован в систему и готов к использованию:

1. ✅ **База данных** - миграция применена
2. ✅ **Backend API** - все эндпоинты работают
3. ✅ **Frontend** - компонент создан и интегрирован
4. ✅ **Навигация** - ссылка добавлена в сайдбар
5. ✅ **Переводы** - поддержка трех языков
6. ✅ **Безопасность** - защита всех эндпоинтов

### 🎯 Как использовать

1. **Войдите в систему** с существующим аккаунтом
2. **Нажмите "Личный кабинет"** в сайдбаре
3. **Заполните профиль** - добавьте недостающую информацию
4. **Загрузите аватар** - кликните на камеру рядом с аватаром
5. **Измените пароль** - в разделе "Безопасность"
6. **Управляйте настройками** - во вкладке "Настройки"

Система готова к работе! 🎉
